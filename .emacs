(setq make-backup-files nil) ; stop creating those backup~ files
(setq auto-save-default nil) ; stop creating those #autosave# files
; how do i make this option platform specific?
(set-default-font "DejaVu Sans Mono-8")
